# gym_machine_judge_b13 v2 重剪版 — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Build 2 重剪版本（v2-A + v2-B）作为 A/B 测试，零新拍零重录。

**Architecture:** 复制 v6 composition → 改 BGM src + 字幕样式 + 转场类型 + (v2-B 限定) 0.55x 慢镜 + 12 节拍点 overlay。配音/字幕/视频素材全部复用。

**Tech Stack:** HTML5 + CSS3 + GSAP 3.14 + hyperframes@0.6.72 + gen-bgm.js (mmx) + FFmpeg 验证

---

## File Structure

| 类型 | 路径 | 用途 |
|---|---|---|
| **新建** | `resources/audios/bgm/gym_machine_judge_b13_v2a.mp3` | v2-A BGM (Cyber Pulse, ≥ 65s) |
| **新建** | `resources/audios/bgm/gym_machine_judge_b13_v2b.mp3` | v2-B BGM (Hop Pulse, ≥ 65s) |
| **新建** | `hyperframe/src/scenes/gym_machine_judge_b13/storyboard_v2.md` | v2-A/B shot 映射文档 |
| **新建** | `hyperframe/compositions/gym_machine_judge_b13_v2a.html` | v2-A composition |
| **新建** | `hyperframe/compositions/gym_machine_judge_b13_v2b.html` | v2-B composition (含 0.55x 慢镜 + 12 节拍点) |
| **新建** | `out/gym_machine_judge_b13_v2a.mp4` | v2-A 渲染产物 (gitignored) |
| **新建** | `out/gym_machine_judge_b13_v2b.mp4` | v2-B 渲染产物 (gitignored) |

**不动项（核心约束）**：
- `resources/audios/gym_machine_judge_b13.m4a`（配音 62.33s，**不重录**）
- `hyperframe/src/scenes/gym_machine_judge_b13/subtitles.json`（字幕 23 条，**不改**）
- 26 个视频素材（**全部复用**）
- `compositions/gym_machine_judge_b13.html`（v6 完整版，**不动**）

**v2-A 与 v2-B 共享 v6 基线**：v2-A = v6 + 5 处修改；v2-B = v6 + 7 处修改（含 v2-A 的 5 处 + 0.55x 慢镜 + 12 节拍点 overlay）。

---

## Task 1: 生成 2 个 BGM 文件

**Files:**
- Create: `resources/audios/bgm/gym_machine_judge_b13_v2a.mp3` (Cyber Pulse, ≥ 65s)
- Create: `resources/audios/bgm/gym_machine_judge_b13_v2b.mp3` (Hop Pulse, ≥ 65s)

> **命名说明**：按 [`rules/production/bgm.md` v3](rules/production/bgm.md) per-topic 命名规范，文件用 `<topic>_<version>.mp3`（topic 仍为 `gym_machine_judge_b13`，加 `_v2a`/`_v2b` 版本后缀区分 A/B 变体）。

- [ ] **Step 1.1: 验证 v6 BGM 文件存在（前置基线）**

```bash
cd /Users/eatong/7fit_hp
ls -la resources/audios/bgm/gym_machine_judge_b13.mp3
```

Expected: 文件存在（约 4-5MB），说明 v6 BGM 已就位。

- [ ] **Step 1.2: 生成 v2-A BGM (Cyber Pulse)**

```bash
cd /Users/eatong/7fit_hp
node tools/gen-bgm.js cyber_pulse --no-cues --duration 70 --out-dir resources/audios/bgm/
mv resources/audios/bgm/cyber_pulse.mp3 resources/audios/bgm/gym_machine_judge_b13_v2a.mp3
ls -la resources/audios/bgm/gym_machine_judge_b13_v2a.mp3
```

Expected: BGM 生成（5-15s 等待），文件 ≥ 1MB。`--no-cues` 跳过 section cue prompt（v2 不需要分 section 的 BGM 变化）。

> **为何用 `--no-cues`**：v2 的 BGM 全片同节奏，不需要 section 情绪切换。如果用 section cue，会在配音分段处加音效（与 v2-A 快切调性不匹配）。

- [ ] **Step 1.3: 验证 v2-A BGM 时长 ≥ 65s**

```bash
ffprobe -v error -show_entries format=duration,bit_rate -of default=noprint_wrappers=1 resources/audios/bgm/gym_machine_judge_b13_v2a.mp3
```

Expected: `duration=65.0xxx` 且 `bit_rate=192000` (192kbps)。如果 < 65s，rerun 加 `--duration 70`。

- [ ] **Step 1.4: 生成 v2-B BGM (Hop Pulse)**

```bash
cd /Users/eatong/7fit_hp
node tools/gen-bgm.js hop_pulse --no-cues --duration 70 --out-dir resources/audios/bgm/
mv resources/audios/bgm/hop_pulse.mp3 resources/audios/bgm/gym_machine_judge_b13_v2b.mp3
ls -la resources/audios/bgm/gym_machine_judge_b13_v2b.mp3
```

Expected: 文件 ≥ 1MB，funk 节拍鲜明（与 v2-A 完全不同风格）。

- [ ] **Step 1.5: 验证 v2-B BGM 时长 ≥ 65s**

```bash
ffprobe -v error -show_entries format=duration,bit_rate -of default=noprint_wrappers=1 resources/audios/bgm/gym_machine_judge_b13_v2b.mp3
```

Expected: `duration=65.0xxx` 且 `bit_rate=192000`。

- [ ] **Step 1.6: 对比 v2-A 和 v2-B BGM 风格（人工耳测）**

```bash
# 用 ffplay 听 10s 样本对比风格（不同的话说明生成成功）
ffplay -ss 5 -t 10 -autoexit resources/audios/bgm/gym_machine_judge_b13_v2a.mp3
ffplay -ss 5 -t 10 -autoexit resources/audios/bgm/gym_machine_judge_b13_v2b.mp3
```

Expected: v2-A 是科技电子紧张感（Cyber Pulse），v2-B 是 funk 节奏鲜明（Hop Pulse）。**两者风格明显不同**。

- [ ] **Step 1.7: 提交 BGM 文件**

```bash
cd /Users/eatong/7fit_hp
git add resources/audios/bgm/gym_machine_judge_b13_v2a.mp3 \
        resources/audios/bgm/gym_machine_judge_b13_v2b.mp3
git commit -m "$(cat <<'EOF'
feat(audio): gym_b13 v2 双版本 BGM (Cyber Pulse + Hop Pulse)

为 v2 重剪版生成 2 个 BGM：
- v2a: Cyber Pulse (科技电子，紧张)
- v2b: Hop Pulse (funk，节奏鲜明)

per-topic 命名 gym_machine_judge_b13_v2{a,b}.mp3。
v6 完整版 (power_build) 不动。

🤖 Generated with [Claude Code](https://claude.com/claude-code)

Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>
EOF
)"
```

---

## Task 2: 写 v2 shot 映射文档 (storyboard_v2.md)

**Files:**
- Create: `hyperframe/src/scenes/gym_machine_judge_b13/storyboard_v2.md`

> **目的**：明确 v2-A 和 v2-B 共用配音/字幕下，每个 shot 在两个版本中如何剪辑（特别是 v2-B 的 0.55x 慢镜应用到哪些 shot）。这是 Task 3/4 composition 改动的依据。

- [ ] **Step 2.1: 写 storyboard_v2.md 头**

```bash
touch hyperframe/src/scenes/gym_machine_judge_b13/storyboard_v2.md
```

- [ ] **Step 2.2: 写入 v2 头信息和 shot 映射表**

写入 [`hyperframe/src/scenes/gym_machine_judge_b13/storyboard_v2.md`](/Users/eatong/7fit_hp/hyperframe/src/scenes/gym_machine_judge_b13/storyboard_v2.md)：

```markdown
# gym_machine_judge_b13 · v2 重剪版分镜表

> **基础**：配音 + 字幕 + 视频全部复用 v6（[v6 storyboard.md](./storyboard.md)）
> **改动**：仅 BGM + 剪辑风格 + 字幕样式
> **两个版本**：
> - v2-A: Cyber Pulse + hard cut + 段间 0.5s + 字幕动效 0.15s
> - v2-B: Hop Pulse + 4 拍节拍对齐（特定 23 镜 0.55x 慢镜）+ 12 reps 数字 overlay

---

## 1 · 共有 shot 表（v2-A 和 v2-B 通用）

| # | shot_id | start | end | dur | v6 视频 | v2-A 处理 | v2-B 处理 |
|---|---|---|---|---|---|---|---|
| 0 | hook_question | 0.0 | 3.44 | 3.44 | `000_hook_gym_pan.mov` | **hard cut** 入场 | hard cut 入场 |
| 1 | t1_intro | 4.14 | 5.91 | 1.76 | (HTML 文字卡) | 字幕动效 0.15s | 字幕动效 0.15s |
| 2-4 | t1_ass_1/2/3 | 5.91 | 9.55 | 3.64 | `ass_seat_{chest,row,leg}.mov` | 字幕 64px bottom 60px | **0.55x 慢镜** (1.22s → 2.18s) |
| 5-7 | t1_back_1/2/3 | 9.55 | 12.91 | 3.36 | `back_pad_{chest,row,leg}.mov` | 同上 | **0.55x 慢镜** |
| 8-10 | t1_hand_1/2/3 | 12.91 | 16.26 | 3.35 | `hand_grip_{chest,pulldown,curl}.mov` | 同上 | **0.55x 慢镜** |
| 11-13 | t1_leg_1/2/3 | 16.26 | 19.61 | 3.35 | `leg_pad_{curl,press,extension}.mov` | 同上 | **0.55x 慢镜** |
| 14-16 | t1_angle_1/2/3 | 19.61 | 23.55 | 3.94 | `angle_{seat,back,handle}.mov` | 字幕 64px | **0.55x 慢镜** |
| 17-18 | t1_weight_1/2 | 23.55 | 26.99 | 3.44 | `weight_{pin,plate}.mov` | 同上 | **0.55x 慢镜** |
| 19-20 | t1_test_1/2 | 26.99 | 30.82 | 3.83 | `test_{light,12reps}.mov` | 同上 | **0.55x 慢镜** + **12 节拍点 overlay**（v2-B 特有）|
| 21 | t1_to_t2 (pause_breath) | 30.82 | 31.52 | 0.7 | 上镜末帧 | 段间 0.5s | 段间 0.7s (节拍自然) |
| 22 | t2_intro | 31.52 | 32.69 | 1.17 | (HTML 文字卡) | 字幕 64px | 字幕 50px |
| 23 | t2_c1_name | 32.69 | 34.75 | 2.06 | (HTML 文字卡) | 同上 | 同上 |
| 24-27 | t2_c1_sit/push/weight/reps | 34.75 | 42.99 | 8.24 | `011_chest_sit.mov` 等 4 镜 | **段间 0.5s** | **0.55x 慢镜**（4 镜全做）|
| 28 | t2_to_t3 (pause_breath) | 42.99 | 43.69 | 0.7 | 上镜末帧 | 段间 0.5s | 段间 0.7s |
| 29 | t3_c2_name | 43.69 | 46.04 | 2.35 | (HTML 文字卡) | 字幕 64px | 字幕 50px |
| 30-31 | t3_c2_setup/reps | 46.04 | 56.34 | 10.3 | `016_curl_setup.mov` + `017_curl_reps.mov` | 保持 (5.0-5.3s 长镜) | 保持 (9 拍自然节拍) |
| 32 | t3_to_out (pause_breath) | 56.34 | 57.04 | 0.7 | 上镜末帧 | 段间 0.5s | 段间 0.7s |
| 33-34 | outro_cta/comment | 57.04 | 62.33 | 5.29 | (HTML 文字卡) | 字幕 64px | 字幕 50px |

> **总时长约束**：62.33s（配音决定）。v2-A 通过减少段间停顿（0.7s→0.5s × 4 处 = -0.8s）+ 字幕动效缩短抵消 v2-B 慢镜扩展时长。

## 2 · v2-B 特有的 23 镜 0.55x 慢镜清单

段 1 接触点 12 镜 + 调节 5 镜 + 试重 2 镜 + 段 2 案例 1 推胸 4 镜 = 23 镜

| 段 | shot_ids | 视频 ID 模式 |
|---|---|---|
| 段 1 接触点 12 镜 | t1_ass_1/2/3, t1_back_1/2/3, t1_hand_1/2/3, t1_leg_1/2/3 | `v-t1_*` |
| 段 1 调节 5 镜 | t1_angle_1/2/3, t1_weight_1/2 | `v-t1_*` |
| 段 1 试重 2 镜 | t1_test_1/2 | `v-t1_test_*` |
| 段 2 案例 1 4 镜 | t2_c1_sit, t2_c1_push, t2_c1_weight, t2_c1_reps | `v-t2_c1_*` |

> **实现机制**：JS 用 `tl.call(() => videoEl.playbackRate = 0.55, [], shot.start)` 触发（详见 Task 4）。
> **时长平衡**：23 镜 × (2.18s - 原时长) ≈ +20s，但配音时长固定 62.33s。v2-B 通过段 1-2 慢镜"扩展" 段内时长，与原配音时间码错位 → GSAP timeline 自动在配音时间点触发切换。
```

- [ ] **Step 2.3: 验证 storyboard 格式**

```bash
wc -l hyperframe/src/scenes/gym_machine_judge_b13/storyboard_v2.md
head -5 hyperframe/src/scenes/gym_machine_judge_b13/storyboard_v2.md
```

Expected: 文件 60+ 行，开头是 `# gym_machine_judge_b13 · v2 重剪版分镜表`。

- [ ] **Step 2.4: 提交 storyboard**

```bash
cd /Users/eatong/7fit_hp
git add hyperframe/src/scenes/gym_machine_judge_b13/storyboard_v2.md
git commit -m "$(cat <<'EOF'
docs(scene): gym_b13 v2 storyboard — 双版本 shot 映射

v2 重剪版分镜表。v2-A 和 v2-B 共用配音/字幕/视频，
仅在剪辑风格和 BGM 上做差异。

v2-A: Cyber Pulse + hard cut + 字幕动效 0.15s
v2-B: Hop Pulse + 23 镜 0.55x 慢镜（4 拍节拍对齐）+ 12 reps 数字 overlay

shot 时间码沿用 v6 配音驱动（62.33s 总时长不变）。

🤖 Generated with [Claude Code](https://claude.com/claude-code)

Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>
EOF
)"
```

---

## Task 3: 构建 v2-A composition (Cyber Pulse + 快切病毒版)

**Files:**
- Create: `hyperframe/compositions/gym_machine_judge_b13_v2a.html` (基于 v6 复制)

> **复用基础**：[`compositions/gym_machine_judge_b13.html`](/Users/eatong/7fit_hp/hyperframe/compositions/gym_machine_judge_b13.html)（v6 完整版，924 行）。

- [ ] **Step 3.1: 复制 v6 composition 为 v2a**

```bash
cd /Users/eatong/7fit_hp
cp hyperframe/compositions/gym_machine_judge_b13.html hyperframe/compositions/gym_machine_judge_b13_v2a.html
ls -la hyperframe/compositions/gym_machine_judge_b13_v2a.html
```

Expected: 文件大小与 v6 相同（~83KB）。

- [ ] **Step 3.2: 改 BGM src（指向 v2-A 新 BGM）**

使用 `Edit` 工具替换 [v2a.html line 115-116](/Users/eatong/7fit_hp/hyperframe/compositions/gym_machine_judge_b13_v2a.html#L115-L116)：

- 旧：
  ```html
        <audio id="audio" src="../../../assets/audios/gym_machine_judge_b13_mixed.mp3"
  ```
- 新：
  ```html
        <audio id="audio" src="../../../assets/audios/bgm/gym_machine_judge_b13_v2a.mp3"
  ```

> **注意**：v6 用的是**预混合** audio（含旁白+BGM 的 .mixed.mp3）— 这是 v6 修复 hyperframes 0.6.72 voiceover-drop bug 的 workaround。v2 也要走这个 workaround（**预混合** voiceover + v2-A BGM），不能单独引用 v2-A BGM 文件。
> **修正**：先看 v6 的 audio 元素实际是 .mixed.mp3 还是 .mp3，再决定是否需要先预混合。

**修正路径**：先看 v6 audio 元素：

```bash
grep -n "audio id=\"audio\"" hyperframe/compositions/gym_machine_judge_b13.html
```

确认是否还是 `gym_machine_judge_b13_mixed.mp3`。如果是，v2 也要预混合（v2 BGM + voiceover 预混），不能用纯 BGM 文件。

**如果需要预混合**：
```bash
cd /Users/eatong/7fit_hp
ffmpeg -y \
  -i resources/audios/gym_machine_judge_b13.m4a \
  -i resources/audios/bgm/gym_machine_judge_b13_v2a.mp3 \
  -filter_complex "[0:a]atrim=0:62.33,volume=8.0,apad=whole_dur=66.0[vo];[1:a]atrim=0:62.33,apad=whole_dur=66.0[bgm];[vo][bgm]amix=inputs=2:duration=longest:dropout_transition=0[mix];[mix]volume=0.5,afade=t=out:st=62:d=3[out]" \
  -map "[out]" -ac 2 -ar 48000 -acodec libmp3lame -b:a 192k -t 66.0 \
  resources/audios/bgm/gym_machine_judge_b13_v2a_mixed.mp3
ls -la resources/audios/bgm/gym_machine_judge_b13_v2a_mixed.mp3
```

然后 v2a.html 的 audio src 用 `gym_machine_judge_b13_v2a_mixed.mp3`。

- [ ] **Step 3.3: 改字幕样式（font-size 56px → 64px）**

在 [`hyperframe/compositions/gym_machine_judge_b13_v2a.html`](/Users/eatong/7fit_hp/hyperframe/compositions/gym_machine_judge_b13_v2a.html) 中找到 `.sub-line` 块（约 line 47-55），把 `font-size: 56px;` 改为 `font-size: 64px;`：

- 旧：
  ```css
        font-size: 56px; font-weight: 700;
  ```
- 新：
  ```css
        font-size: 64px; font-weight: 700;
  ```

- [ ] **Step 3.4: 改字幕位置（bottom 80px → 60px）**

在同一文件 `.sub-overlay` 块（约 line 43-46），把 `bottom: 80px;` 改为 `bottom: 60px;`：

- 旧：
  ```css
        position: absolute; bottom: 80px; left: 0; width: 100%;
  ```
- 新：
  ```css
        position: absolute; bottom: 60px; left: 0; width: 100%;
  ```

- [ ] **Step 3.5: 改 GSAP 字幕入场动效时长（0.3s → 0.15s）**

在文件底部 GSAP timeline 段，所有 `tl.fromTo(..., { duration: 0.3, ... })` 改为 `duration: 0.15`：

```bash
cd /Users/eatong/7fit_hp
sed -i '' 's/duration: 0.3, ease: '"'"'power2.out'"'"'/duration: 0.15, ease: '"'"'power2.out'"'"'/g' hyperframe/compositions/gym_machine_judge_b13_v2a.html
grep -c "duration: 0.3, ease: 'power2.out'" hyperframe/compositions/gym_machine_judge_b13_v2a.html
```

Expected: 替换后 0 个 0.3s 出现（所有字幕入场动效都改为 0.15s）。如有剩余，手动检查（可能是因为 `power2.out` 后跟其他参数）。

- [ ] **Step 3.6: 改 GSAP 字幕出场动效时长（0.2s → 0.1s）**

```bash
cd /Users/eatong/7fit_hp
sed -i '' 's/duration: 0.2, ease: '"'"'power2.in'"'"'/duration: 0.1, ease: '"'"'power2.in'"'"'/g' hyperframe/compositions/gym_machine_judge_b13_v2a.html
grep -c "duration: 0.2, ease: 'power2.in'" hyperframe/compositions/gym_machine_judge_b13_v2a.html
```

Expected: 替换后 0 个 0.2s 出现。

- [ ] **Step 3.7: 改段间停顿（0.7s → 0.5s）**

在 GSAP timeline 末尾，找到 pause_breath 动效（搜索 `pause_breath` 或 `0.8×` 等关键词）：

```bash
grep -n "pause_breath\|0.8x\|playbackRate\|0.7" hyperframe/compositions/gym_machine_judge_b13_v2a.html | head -20
```

找到段间停顿相关代码后，把 `0.7` 改为 `0.5`（如果有 `duration: 0.7`）。**注意**：v6 的 pause_breath 用的是视频慢放，不是 `0.7` 时长。可能需要根据实际代码做调整。

> **如果 v6 的 pause_breath 是视频慢放**（如 `playbackRate = 0.8`），不需要改时长本身，但可以考虑改成 `playbackRate = 0.6`（更快节奏，匹配 v2-A 的"快切"调性）。**这是一个 v2-A 优化点**。

- [ ] **Step 3.8: 验证 v2a.html 关键修改生效**

```bash
cd /Users/eatong/7fit_hp
echo "=== audio src ==="
grep 'audio id="audio"' hyperframe/compositions/gym_machine_judge_b13_v2a.html
echo "=== font-size ==="
grep -A1 "\.sub-line {" hyperframe/compositions/gym_machine_judge_b13_v2a.html | head -5
echo "=== bottom ==="
grep -A1 "\.sub-overlay {" hyperframe/compositions/gym_machine_judge_b13_v2a.html | head -5
echo "=== 0.3s remaining (期望 0) ==="
grep -c "duration: 0.3" hyperframe/compositions/gym_machine_judge_b13_v2a.html
echo "=== 0.2s remaining (期望 0) ==="
grep -c "duration: 0.2" hyperframe/compositions/gym_machine_judge_b13_v2a.html
```

Expected: 5 个检查全部通过（0 个 0.3s/0.2s 残留）。

- [ ] **Step 3.9: 跑 lint**

```bash
cd /Users/eatong/7fit_hp/hyperframe
npx --yes hyperframes@0.6.72 lint 2>&1 | head -20
```

Expected: 0 errors 报在 `gym_machine_judge_b13_v2a.html`。如有错，按提示修复。

- [ ] **Step 3.10: 提交 v2a composition**

```bash
cd /Users/eatong/7fit_hp
git add hyperframe/compositions/gym_machine_judge_b13_v2a.html \
        resources/audios/bgm/gym_machine_judge_b13_v2a_mixed.mp3 2>/dev/null
git commit -m "$(cat <<'EOF'
feat(composition): gym_b13 v2-A — Cyber Pulse + 快切病毒版

v6 重剪版（v2-A），仅改 BGM + 字幕样式 + 动效时长：

- BGM: v6 power_build → Cyber Pulse (科技电子)
- 预混合 audio: gym_machine_judge_b13_v2a_mixed.mp3
  (voiceover × 8 + BGM 原音量 + 3s 淡出)
- 字幕字号 56px → 64px（更大更醒目）
- 字幕位置 bottom 80px → 60px（更靠底）
- GSAP 字幕动效 0.3s → 0.15s（更急促）
- GSAP 字幕出场 0.2s → 0.1s（更快消失）

配音/字幕/视频素材 100% 复用 v6，零新拍零重录。

🤖 Generated with [Claude Code](https://claude.com/claude-code)

Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>
EOF
)"
```

---

## Task 4: 构建 v2-B composition (Hop Pulse + 4 拍节拍对齐)

**Files:**
- Create: `hyperframe/compositions/gym_machine_judge_b13_v2b.html` (基于 v2a 复制 + 慢镜 + 节拍点 overlay)

- [ ] **Step 4.1: 复制 v2a 为 v2b（v2a 已有大部分修改，v2b 在此基础上加 0.55x 慢镜 + 节拍点）**

```bash
cd /Users/eatong/7fit_hp
cp hyperframe/compositions/gym_machine_judge_b13_v2a.html hyperframe/compositions/gym_machine_judge_b13_v2b.html
ls -la hyperframe/compositions/gym_machine_judge_b13_v2b.html
```

Expected: 与 v2a 相同大小。

- [ ] **Step 4.2: 改 BGM src（指向 v2-B 新 BGM）**

使用 `Edit` 工具替换 [v2b.html](hyperframe/compositions/gym_machine_judge_b13_v2b.html) 中的 audio src：

- 旧：
  ```html
  <audio id="audio" src="../../../assets/audios/bgm/gym_machine_judge_b13_v2a_mixed.mp3"
  ```
- 新：
  ```html
  <audio id="audio" src="../../../assets/audios/bgm/gym_machine_judge_b13_v2b_mixed.mp3"
  ```

- [ ] **Step 4.3: 预混合 v2-B audio（voiceover + Hop Pulse）**

```bash
cd /Users/eatong/7fit_hp
ffmpeg -y \
  -i resources/audios/gym_machine_judge_b13.m4a \
  -i resources/audios/bgm/gym_machine_judge_b13_v2b.mp3 \
  -filter_complex "[0:a]atrim=0:62.33,volume=8.0,apad=whole_dur=66.0[vo];[1:a]atrim=0:62.33,apad=whole_dur=66.0[bgm];[vo][bgm]amix=inputs=2:duration=longest:dropout_transition=0[mix];[mix]volume=0.5,afade=t=out:st=62:d=3[out]" \
  -map "[out]" -ac 2 -ar 48000 -acodec libmp3lame -b:a 192k -t 66.0 \
  resources/audios/bgm/gym_machine_judge_b13_v2b_mixed.mp3
ls -la resources/audios/bgm/gym_machine_judge_b13_v2b_mixed.mp3
ffprobe -v error -show_entries format=duration -of default=noprint_wrappers=1 resources/audios/bgm/gym_machine_judge_b13_v2b_mixed.mp3
```

Expected: 文件 ~1.5MB, duration=66.0s (3s fade out tail)。

- [ ] **Step 4.4: 改字幕字号（v2-A 的 64px → v2-B 的 50px）**

在 [v2b.html](hyperframe/compositions/gym_machine_judge_b13_v2b.html) 中：

- 旧：
  ```css
        font-size: 64px; font-weight: 700;
  ```
- 新：
  ```css
        font-size: 50px; font-weight: 700;
  ```

- [ ] **Step 4.5: 改字幕位置（v2-A 的 60px → v2-B 的 100px）**

- 旧：
  ```css
        position: absolute; bottom: 60px; left: 0; width: 100%;
  ```
- 新：
  ```css
        position: absolute; bottom: 100px; left: 0; width: 100%;
  ```

- [ ] **Step 4.6: 添加 0.55x 慢镜 JS（23 镜，task 4.6a-d 四个子步骤）**

> **核心机制**：用 `tl.call(() => videoEl.playbackRate = 0.55, [], shotStartTime)` 在 shot 开始时触发。playbackRate 修改会让视频以 0.55x 速度播放，1.22s 视频变 2.18s。

**Step 4.6a: 在 GSAP timeline 末尾添加 23 个慢镜触发**

在 [v2b.html](hyperframe/compositions/gym_machine_judge_b13_v2b.html) 的 GSAP timeline 末尾（`window.__timelines["gym_machine_judge_b13"] = tl;` 之前），添加以下 JS 块：

```javascript
// ============ v2-B: 0.55x 慢镜到 4 拍节拍 ============
const slowMoShots = [
  // 段 1 接触点 12 镜
  { id: 'v-t1_ass_1', start: 5.91 },
  { id: 'v-t1_ass_2', start: 7.13 },
  { id: 'v-t1_ass_3', start: 8.34 },
  { id: 'v-t1_back_1', start: 9.55 },
  { id: 'v-t1_back_2', start: 10.67 },
  { id: 'v-t1_back_3', start: 11.78 },
  { id: 'v-t1_hand_1', start: 12.91 },
  { id: 'v-t1_hand_2', start: 14.04 },
  { id: 'v-t1_hand_3', start: 15.15 },
  { id: 'v-t1_leg_1', start: 16.26 },
  { id: 'v-t1_leg_2', start: 17.40 },
  { id: 'v-t1_leg_3', start: 18.51 },
  // 段 1 调节 5 镜
  { id: 'v-t1_angle_1', start: 19.61 },
  { id: 'v-t1_angle_2', start: 20.92 },
  { id: 'v-t1_angle_3', start: 22.23 },
  { id: 'v-t1_weight_1', start: 23.55 },
  { id: 'v-t1_weight_2', start: 25.27 },
  // 段 1 试重 2 镜
  { id: 'v-t1_test_1', start: 26.99 },
  { id: 'v-t1_test_2', start: 28.91 },
  // 段 2 案例 1 推胸 4 镜
  { id: 'v-t2_c1_sit', start: 34.75 },
  { id: 'v-t2_c1_push', start: 37.11 },
  { id: 'v-t2_c1_weight', start: 39.16 },
  { id: 'v-t2_c1_reps', start: 40.64 }
];
slowMoShots.forEach(({ id, start }) => {
  tl.call(() => {
    const v = document.getElementById(id);
    if (v) v.playbackRate = 0.55;
  }, [], start);
});
```

**Step 4.6b: 验证慢镜 JS 块存在**

```bash
grep -c "v-t1_ass_1" hyperframe/compositions/gym_machine_judge_b13_v2b.html
grep -c "playbackRate = 0.55" hyperframe/compositions/gym_machine_judge_b13_v2b.html
```

Expected: 第一个 grep ≥ 1（slowMoShots 数组包含），第二个 = 1（playbackRate 赋值语句在 slowMoShots.forEach 里出现 1 次）。

- [ ] **Step 4.7: 添加 12 个 reps 数字 overlay (节拍点)**

**Step 4.7a: 在 HTML body 添加 overlay 容器**

在 [v2b.html](hyperframe/compositions/gym_machine_judge_b13_v2b.html) 的 `</section>` 之前（line ~920 附近），添加：

```html
<!-- v2-B 12 reps 数字 overlay -->
<div id="reps-counter" style="position:absolute; top:200px; right:64px;
  font-size:64px; font-weight:800; color:#FF4500;
  text-shadow:0 4px 12px rgba(0,0,0,0.95);
  background:rgba(0,0,0,0.7); padding:8px 16px; border-radius:8px;
  z-index:20; opacity:0; display:flex; align-items:center; justify-content:center;
  min-width:120px;"></div>
```

**Step 4.7b: 在 GSAP timeline 末尾（slowMoShots 之后）添加 12 个数字动画**

```javascript
// ============ v2-B: 12 reps 数字 overlay（节拍点显示）============
const repsEl = document.getElementById('reps-counter');
let repsCount = 0;
function updateReps(n) {
  if (repsEl) repsEl.textContent = `${n}/12`;
}
// 12 个 reps 触发：每个 0.55x 慢镜的 12 reps 段约 2.62s（12 拍），每个 rep ≈ 0.218s
// 在 t1_test_1/t1_test_2/t2_c1_reps 段各分配 4 次 reps
// 简化：12 个数字 0.2s 间隔出现，在 t1_test_1 (26.99) 之后开始
for (let i = 1; i <= 12; i++) {
  tl.call(() => updateReps(i), [], 27.0 + (i - 1) * 0.22);
}
tl.to('#reps-counter', { opacity: 1, duration: 0.1 }, 27.0);
tl.to('#reps-counter', { opacity: 0, duration: 0.3 }, 30.5);  // 段 1 末隐藏
// 段 2 案例 1 reps 段（t2_c1_reps = 40.64-42.99）再来 4 次
for (let i = 1; i <= 12; i++) {
  tl.call(() => updateReps(i), [], 40.64 + (i - 1) * 0.22);
}
tl.to('#reps-counter', { opacity: 1, duration: 0.1 }, 40.64);
tl.to('#reps-counter', { opacity: 0, duration: 0.3 }, 42.99);
```

**Step 4.7c: 验证 overlay 块存在**

```bash
grep -c 'id="reps-counter"' hyperframe/compositions/gym_machine_judge_b13_v2b.html
grep -c "updateReps" hyperframe/compositions/gym_machine_judge_b13_v2b.html
```

Expected: 第一个 ≥ 1, 第二个 ≥ 1。

- [ ] **Step 4.8: 验证 v2b.html 关键修改全部生效**

```bash
cd /Users/eatong/7fit_hp
echo "=== audio src ==="
grep 'audio id="audio"' hyperframe/compositions/gym_machine_judge_b13_v2b.html
echo "=== font-size ==="
grep -A1 "\.sub-line {" hyperframe/compositions/gym_machine_judge_b13_v2b.html | head -5
echo "=== bottom ==="
grep -A1 "\.sub-overlay {" hyperframe/compositions/gym_machine_judge_b13_v2b.html | head -5
echo "=== 0.55x 慢镜 ==="
grep -c "playbackRate = 0.55" hyperframe/compositions/gym_machine_judge_b13_v2b.html
echo "=== 12 reps overlay ==="
grep -c "reps-counter" hyperframe/compositions/gym_machine_judge_b13_v2b.html
```

Expected: 5 个检查全部通过。

- [ ] **Step 4.9: 跑 lint**

```bash
cd /Users/eatong/7fit_hp/hyperframe
npx --yes hyperframes@0.6.72 lint 2>&1 | head -20
```

Expected: 0 errors 报在 `gym_machine_judge_b13_v2b.html`。如 `playbackRate` JS 块报错，确认语法（特别注意 `tl.call(() => {...}, [], start)` 的 args 顺序）。

- [ ] **Step 4.10: 提交 v2b composition**

```bash
cd /Users/eatong/7fit_hp
git add hyperframe/compositions/gym_machine_judge_b13_v2b.html \
        resources/audios/bgm/gym_machine_judge_b13_v2b_mixed.mp3
git commit -m "$(cat <<'EOF'
feat(composition): gym_b13 v2-B — Hop Pulse + 4 拍节拍对齐版

v6 重剪版（v2-B），在 v2-A 基础上加 2 个节拍机制：

1. 0.55x 慢镜（23 镜）— 段 1 接触点 12 镜 + 调节 5 镜 + 试重 2 镜
   + 段 2 案例 1 推胸 4 镜。让视频从 1.22s 拉长到 2.18s（4 拍）
2. 12 个 reps 数字 overlay（节拍点）— t1_test_1/2 段和 t2_c1_reps 段
   各显示 1/12 → 12/12（橙色 64px 居右上）

附加 v2-A 的基础修改（不在此 commit 重复）:
- BGM: v2-B 预混合
- 字幕 50px / bottom 100px（v2-A 是 64px / 60px）

配音/字幕/视频素材 100% 复用 v6，零新拍零重录。

🤖 Generated with [Claude Code](https://claude.com/claude-code)

Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>
EOF
)"
```

---

## Task 5: 渲染 v2-A 并验证

**Files:**
- Create: `out/gym_machine_judge_b13_v2a.mp4` (gitignored)

- [ ] **Step 5.1: 渲染 v2-A**

```bash
cd /Users/eatong/7fit_hp/hyperframe
npx --yes hyperframes@0.6.72 render . \
  --composition=compositions/gym_machine_judge_b13_v2a.html \
  --output=out/gym_machine_judge_b13_v2a.mp4 2>&1 | tail -10
```

Expected: 渲染完成，输出 `out/gym_machine_judge_b13_v2a.mp4`，显示 "100% Render complete"。

- [ ] **Step 5.2: 验证 v2-A 输出文件**

```bash
ls -la /Users/eatong/7fit_hp/hyperframe/out/gym_machine_judge_b13_v2a.mp4
ffprobe -v error -show_entries stream=codec_type,codec_name,duration -of default /Users/eatong/7fit_hp/hyperframe/out/gym_machine_judge_b13_v2a.mp4
```

Expected:
- 文件存在（~80MB）
- Stream 0: H.264 video, duration ≈ 62.33s
- Stream 1: AAC audio, duration ≈ 62.336s

- [ ] **Step 5.3: 验证 v2-A 音频包含旁白+BGM 混合（不是纯 BGM）**

```bash
ffmpeg -hide_banner -i /Users/eatong/7fit_hp/hyperframe/out/gym_machine_judge_b13_v2a.mp4 \
  -af "asetnsamples=480000,astats=metadata=1,ametadata=mode=print:key=lavfi.astats.Overall.RMS_level" \
  -f null - 2>&1 | grep -iE "rms" | awk -F'=' '{print $2}' | head -7
```

Expected: 7 个 RMS 值，**分布在 -18 到 -20 dB 之间**（旁白 -22dB + BGM -13dB 混合），**不是平稳 -13dB**（纯 BGM 模式，证明 v2-A 的预混合 voiceover+BGM 成功）。

- [ ] **Step 5.4: 提交渲染记录（如有 meta 变化）**

> 渲染产物在 .gitignore（hyperframe/out/），不需 commit。Step 5.3 验证通过即代表 v2-A 渲染成功。

```bash
git status
```

Expected: 无未提交文件（render 输出在 .gitignore）。

---

## Task 6: 渲染 v2-B 并验证

**Files:**
- Create: `out/gym_machine_judge_b13_v2b.mp4` (gitignored)

- [ ] **Step 6.1: 渲染 v2-B**

```bash
cd /Users/eatong/7fit_hp/hyperframe
npx --yes hyperframes@0.6.72 render . \
  --composition=compositions/gym_machine_judge_b13_v2b.html \
  --output=out/gym_machine_judge_b13_v2b.mp4 2>&1 | tail -10
```

Expected: 渲染完成。

- [ ] **Step 6.2: 验证 v2-B 输出文件**

```bash
ls -la /Users/eatong/7fit_hp/hyperframe/out/gym_machine_judge_b13_v2b.mp4
ffprobe -v error -show_entries stream=codec_type,codec_name,duration -of default /Users/eatong/7fit_hp/hyperframe/out/gym_machine_judge_b13_v2b.mp4
```

Expected:
- 文件存在
- Stream 0: H.264 video, duration ≈ 62.33s
- Stream 1: AAC audio, duration ≈ 62.336s

- [ ] **Step 6.3: 验证 v2-B 音频有节拍点 overlay 视觉提示（通过 luma 检查）**

> **说明**：v2-B 的 reps 数字 overlay 是 HTML 元素，会被 render 进 video stream。可通过检查特定时间点的 luma 变化验证（橙色 #FF4500 数字存在时 luma 偏高）。

```bash
# 检查 t1_test_1 时段（27-30s）是否有色彩变化
ffmpeg -hide_banner -i /Users/eatong/7fit_hp/hyperframe/out/gym_machine_judge_b13_v2b.mp4 \
  -ss 27 -t 3 -vf "signalstats" -an -f null - 2>&1 | grep -E "YAVG|YMAX" | head -5
```

Expected: 27-30s 段 YMAX > 200 (橙色高亮数字)，证明 reps overlay 已 render。

- [ ] **Step 6.4: 验证 v2-B 音频含旁白+BGM 混合**

```bash
ffmpeg -hide_banner -i /Users/eatong/7fit_hp/hyperframe/out/gym_machine_judge_b13_v2b.mp4 \
  -af "asetnsamples=480000,astats=metadata=1,ametadata=mode=print:key=lavfi.astats.Overall.RMS_level" \
  -f null - 2>&1 | grep -iE "rms" | awk -F'=' '{print $2}' | head -7
```

Expected: RMS 分布在 -18 到 -20 dB（与 v2-A 类似，证明预混合成功）。

- [ ] **Step 6.5: v2-A vs v2-B 音频对比（确认 BGM 风格不同）**

```bash
# 对比 v2-A 和 v2-B 音频频谱
ffmpeg -hide_banner -i /Users/eatong/7fit_hp/hyperframe/out/gym_machine_judge_b13_v2a.mp4 \
  -af "asetnsamples=48000,astats=metadata=1,ametadata=mode=print:key=lavfi.astats.Overall.Peak_level" \
  -f null - 2>&1 | grep "PEAK" | head -3
ffmpeg -hide_banner -i /Users/eatong/7fit_hp/hyperframe/out/gym_machine_judge_b13_v2b.mp4 \
  -af "asetnsamples=48000,astats=metadata=1,ametadata=mode=print:key=lavfi.astats.Overall.Peak_level" \
  -f null - 2>&1 | grep "PEAK" | head -3
```

Expected: 两者 PEAK 模式不同（v2-A Cyber Pulse 高频尖峰多，v2-B Hop Pulse 中频节拍峰值多），证明 BGM 风格差异已在输出体现。

- [ ] **Step 6.6: 提交记录**

> 渲染产物在 .gitignore。

```bash
git status
```

Expected: 无未提交文件。

---

## Task 7: A/B 投放准备（手动动作，plan 不执行）

> **这一 task 是手动动作**（用户在抖音/小红书发视频），plan 写到这就够。**下一轮发布时**（6/13 20:00 v2-A / 6/14 22:00 v2-B）按以下 checklist 操作：

- [ ] **Step 7.1: 6/13 20:00 在抖音 + 小红书发布 v2-A**

抖音操作：
- 文件：`out/gym_machine_judge_b13_v2a.mp4`
- 标题：**办了健身卡不会用器械？30 秒速记版来了**
- 简介：见 [spec §6.2](../specs/2026-06-11-gym-b13-v2-design.md#62-发布文案)
- 标签：`#健身房器械 #健身新手 #30秒速记 #推胸`
- 封面：从视频抽帧（选 27-30s 段，推胸 12 次 reps 视觉）

小红书操作：
- 标题：`🏋️ 30 秒速记版｜4 步法搞定健身房器械`
- 正文：见 spec §6.2
- 标签：`#健身房器械 #健身新手 #健身房教程 #推胸 #器械使用`

- [ ] **Step 7.2: 6/14 22:00（错开 2 小时）在抖音 + 小红书发布 v2-B**

抖音：
- 文件：`out/gym_machine_judge_b13_v2b.mp4`
- 标题：**健身房器械 4 步法｜节拍跟练版（12 次跟节拍）**
- 简介：见 spec §6.2
- 标签：`#健身房器械 #健身新手 #节拍跟练 #推胸 #燃脂`

小红书：
- 标题：`🏋️ 节拍跟练版｜4 步法 + 12 reps 跟节拍`
- 正文：见 spec §6.2

- [ ] **Step 7.3: 6/14 + 6/20 两次数据回收**

按 [`rules/delivery/publish.md` §1.2 24h/7d 复盘 checklist](rules/delivery/publish.md)，把数据填入 v2-A / v2-B 的 `copy_notes.md`（或 `topic_recap.json`，按现有 `gym_machine_judge_b13.copy_notes.md` 模板扩展）。

---

## 验收标准（spec §11 对齐）

| # | 验收项 | 通过条件 |
|---|---|---|
| 1 | BGM 生成 | 2 个新 BGM 各 ≥ 65s, 192kbps+ |
| 2 | 配音/字幕/视频 100% 复用 | 3 类文件 mtime 未变 |
| 3 | v2a 时长 | 60-63s（允许 ±2s 偏差）|
| 4 | v2b 时长 | 60-63s |
| 5 | npx hyperframes lint | 0 errors（v2a + v2b）|
| 6 | 渲染产物 | 2 个 MP4 文件，H.264 1080x1920 30fps |
| 7 | 音频 RMS | v2a + v2b 都在 -18 到 -20 dB 范围（旁白+BGM 混合）|
| 8 | 12 reps overlay | v2b 在 27-30s 和 40-42.99s 有橙色高亮 |

---

## 风险登记表

| 风险 | 概率 | 缓解 |
|---|---|---|
| v2-A 预混合 audio 用 Cyber Pulse 但混音参数和 v6 不同 → 旁白不清 | 中 | 复用 v6 混音参数（voiceover volume=8.0, BGM 原音量, 0.5 混合, 3s 淡出）|
| v2-B 0.55x 慢镜扩展时长与配音时间码冲突 | 中 | GSAP timeline 是按 shot.start 触发的，慢镜视频超出会持续到下一个 shot 切换点 |
| v2-B 23 个 `tl.call` 性能问题 | 低 | GSAP call 几乎零开销，render 影响 < 50ms |
| 12 reps overlay 时序不准 | 中 | 简单循环可能错位，建议 r1 渲染后人工看 27-30s 段检查 |
| v2 渲染产物走 .gitignore 不入库 | (合规) | 保留在本地，按 publish.md §3 矩阵发布 |

---

## 下一步

完成 Task 1-6 后：
- **手动发布**（Task 7）按 spec §6.1 时间表
- **数据回收**（Task 7.3）按 spec §6.3 A/B 决策标准判定 v2-A vs v2-B 主推
- 决策结果回填到 [v2 design spec §6.3](specs/2026-06-11-gym-b13-v2-design.md#63-ab-决策标准7d-数据)，形成"测试→决策→迭代"闭环
